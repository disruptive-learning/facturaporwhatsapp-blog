---
# Required fields
title: My Post Title
date: "2026-01-01T00:00:00.000Z"   # ISO 8601 format
description: "A short summary shown in the post header and used for SEO meta tags."

# Optional fields
slug: ""          # Custom URL path, e.g. "/my-custom-slug". Leave empty to derive from filename.
tags: []          # Array of topic tags shown as pills on the post, e.g. ["whatsapp", "facturación"]
author: ""        # Overrides the default site author. Leave empty to use siteMetadata.author.name.
published: false  # Set to false to hide this post from production builds (still visible in dev).
---

Your post content starts here. You can use standard Markdown as well as JSX components.

## A Section Heading

Write your paragraphs, lists, code blocks, and images here as normal Markdown.

```js
// Code blocks are syntax-highlighted via PrismJS
const example = "hello world"
```

You can also import and use React components inline since this is MDX:

{/* import MyComponent from "../../src/components/MyComponent" */}
{/* <MyComponent /> */}
